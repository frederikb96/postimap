apiVersion: v1
kind: Service
metadata:
  name: {{ include "postimap.fullname" . }}
  labels:
    {{- include "postimap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "postimap.selectorLabels" . | nindent 4 }}
