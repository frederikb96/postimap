DB_PASSWORD=
# AES-256-GCM encryption key for IMAP/SMTP credentials (64 hex chars = 32 bytes)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Leave empty to store passwords as plaintext (backwards compatible)
ENCRYPTION_KEY=
